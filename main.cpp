#include ".assignment/test.h"

// 比较三个数的大小
void CompareThreeNumbers(int a, int b, int c, 4& 6)
{
#include <stdio.h>
	int a, b, c;
	int max1(int x, int y);
	int main()
	{
		scanf("%d %d %d", &a, &b, &c);
		int n = max1(a, b);
		printf("%d", max1(n, c));
		return 0;
	}

	int max1(int x, int y)
	{
		int z;
		if (x > y)
			z = x;
		else
			z = y;
		return z;
	}


	result.min = a;
    result.mid = b;
    result.max = c;
}
